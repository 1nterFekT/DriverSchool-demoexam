<nav class="navbar">
    <div class="navbar-container">
        <a href="/" class="logo">
            Автошкола
        </a>

        <div class="nav-links">
            {{#if user}}
                <a href="/profile">Личный кабинет</a>
                <span class="user-badge">
                    {{user.login}}
                </span>
                <a href="/logout">Выйти</a>
            {{/if}}
        </div>
    </div>
</nav>